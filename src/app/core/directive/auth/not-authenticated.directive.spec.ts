import { NotAuthenticatedDirective } from './not-authenticated.directive';

describe('NotAuthenticatedDirective', () => {
  it('should create an instance', () => {
    const directive = new NotAuthenticatedDirective();
    expect(directive).toBeTruthy();
  });
});
